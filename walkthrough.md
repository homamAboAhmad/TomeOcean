# دليل تنفيذ نافذة بيانات الكتاب (Plan 4 Walkthrough)

تهدف هذه التعديلات إلى تحسين تجربة استيراد الكتب في تطبيق **المكتبة الذهبية** من خلال جمع البيانات الوصفية (المؤلف، التصنيف، العنوان) بشكل منظم قبل عملية الاستيراد.

## 1. استخراج البيانات تلقائياً (Metadata Extraction)
تم تحديث كلاس `DocxParser` ليدعم قراءة خصائص ملف الـ Word المعيارية.
- **التعديل**: تم مراجعة ملف `docProps/core.xml` داخل أرشيف `.docx`.
- **البيانات المستخرجة**: 
  - `dc:title`: العنوان المسجل داخل خصائص الملف.
  - `dc:creator`: اسم المؤلف المسجل داخل خصائص الملف.
- **الفائدة**: توفير وقت المستخدم من خلال الاقتراح التلقائي للبيانات الموجودة بالفعل في الملف.

## 2. واجهات المستخدم الجديدة (UI Components)

### أ. نافذة بيانات الكتاب الواحد (`BookMetadataInputDialog`)
تظهر هذه النافذة عند اختيار ملف واحد للاستيراد.
- **الإكمال التلقائي للمؤلف**: بمجرد كتابة أول حروف من اسم المؤلف، يقترح التطبيق الأسماء الموجودة بالفعل في قاعدة البيانات.
- **إضافة مؤلف جديد**: إذا لم يكن المؤلف موجوداً، يمكن للمستخدم تسجيله مع إضافة "سنة الوفاة" (هجرية).
- **إدارة التصنيفات**: اختيار التصنيف من قائمة منسدلة، مع زر مخصص لإضافة تصنيف جديد فورياً دون الخروج من النافذة.

### ب. نافذة البيانات الدفعية (`BatchMetadataInputDialog`)
تظهر عند استيراد مجلد كامل أو عدة ملفات.
- **عرض جدولي**: يتيح رؤية جميع الملفات المختارة وبياناتها في قائمة واحدة.
- **ميزة "التطبيق على الكل"**: يمكن للمستخدم اختيار مؤلف واحد أو تصنيف واحد وتطبيقه على جميع الكتب المختارة بضغطة زر، وهو أمر شديد الأهمية عند استيراد سلاسل الكتب أو التصنيفات الكاملة.

## 3. التكامل التقني (Integration)

- **نظام الفهرسة**: تم ربط `BatchFileProcessingDialog` بالواجهات الجديدة. لم تعد عملية المعالجة تبدأ إلا بعد تأكيد البيانات الوصفية أو إلغائها.
- **حفظ البيانات**: تم تحديث `BookProcessingService` لضمان كتابة بيانات `BookCard` في قاعدة بيانات SQLite (`BooksMetadataDatabase`) فور نجاح عملية الاستيراد والفهرسة.

## 4. كيفية الاستخدام
1. اضغط على "إضافة كتاب" أو "استيراد مجلد" من القائمة الجانبية.
2. اختر الملف/المجلد المطلوب.
3. ستظهر لك النافذة الجديدة (منفردة أو دفعية) لمراجعة البيانات.
4. بعد إدخال البيانات، اضغط "متابعة" أو "بدء الاستيراد".
5. سيبدأ التطبيق في معالجة الكتب وستجدها في مكتبتك مصنفة بشكل صحيح تحت القسم والمؤلف الذي اخترته.

---
*تم التنفيذ وفقاً لمعايير الكود النظيف ومبادئ تجربة المستخدم (Arabic RTL).*
