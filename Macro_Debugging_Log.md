# سجل أخطاء وتحديات الماكرو والقالب (TheLibrary_Helper)

## 1. التحديثات والمسميات (Shamela -> TheLibrary)
تم التعديل الشامل لاسم القالب من `shamela_helper.dotm` إلى `the_library_helper.dotm` والماكرو من `ShamelaPrepareDoc` إلى `TheLibraryPrepareDoc`. وشمل ذلك التعديل كل من الأكواد المستدعاة في `pageRender.py` وكود التوليد في `create_dotm_v2.py`.

## 2. المشكلة الأولى: عدم العثور على القالب بعد البناء
- **المشكلة**: عند تشغيل `pageRender.exe` يطبع "Macro execution skipped (template missing)" لأنه لم يجد القالب.
- **السبب**: عند البناء بـ `PyInstaller` لم نخبره بتضمين القالب.
- **الحل**: تمت إضافة `datas=[('the_library_helper.dotm', '.')]` إلى ملف `pageRender.spec` ليتم حزم القالب واستخراجه في مجلد التطبيق المؤقت.

## 3. المشكلة الأهم والأعقد: 
رسالة الخطأ الشهيرة: `(-2147352567, 'Exception occurred.', 'يتعذر على Word فتح قالب هذا المستند')`
- **الظن الخاطئ**: ظننت أن Microsoft Word يرفض فتح ملف `.dotm` لأنه مستخرج في مجلد التحميل المؤقت `Temp` (Protected View / Trust Center). ولحل ذلك قمت بكتابة كود ينسخ الملف إلى مسار آمن في `%APPDATA%`.
- **النتيجة**: استمر الخطأ نفسه، وأثبت هذا أن المشكلة **ليست في الأذونات أو المسار السري**.
- **السبب الحقيقي والعميق**: 
  ملف `create_dotm_v2.py` كان يقوم بحفظ الملف كالتالي `doc.SaveAs2(dotm_path, 13)`. 
  الرقم 13 (Format=13) يعني في مكتبة Microsoft أنّ هذا الملف هو **مستند ماكرو عادي (`.docm`)** وليس قالب (`.dotm`). 
  وبهذا، نحن نعطي Word مستنداً بهيكلية `docm` ولكن باسم ينتهي بـ `.dotm`. وعندما حاولنا تركيبه بالقوة من خلال البايثون (`doc.AttachedTemplate`)، كان الوورد يرى الخداع الداخلي، فيرسل استثناء الخلل ويرفض تنفيذه.
- **الحل**: تعديل رقم صياغة الحفظ إلى `15` (wdFormatXMLTemplateMacroEnabled). بعدها قام بناء الملف بتوليد قالب سليم ومطابق تماماً هندسياً وداخلياً. تم تنظيف سجلات הבناء والمجلدات المرتبطة به، وتم بناء البرنامج، وعمل بشكل لا تشوبه شائبة.

## 4. دروس مستفادة عن Word COM و PyInstaller 
- **PyInstaller**: الملفات الخارجية لا يتم تجميعها سحرياً. يجب تحديد السجل البياني `datas`.
- **Microsoft Word / COM**: لا تصدق الامتداد السطحي للملف بل بنية هيكله وتجميعه (Format Code). `docm` و `dotm` يشتركان في كونهما XML Macro لكنهما مختلفان جوهرياً.
- **التجميع (Compiling)**: لا يجب البناء على سكربت التوليد القديم الذي قد يحتوي على إعدادات فاسدة. قمنا باستخدام `create_shamela_dotm.py` بعد إصلاحه إلى رقم 15 وحقن الكود الشامل به.
